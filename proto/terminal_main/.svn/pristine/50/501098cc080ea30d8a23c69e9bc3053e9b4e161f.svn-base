package act_pk;	//切磋服务

option py_generic_services=true;

import "universal/public.proto";
import "common.proto";


//上行(客户端主动发起)
service terminal2main{//登录注册账号相关

	rpc rpcActPk(common.int64_)returns(public.fake);//切磋，上传对手id
}

//下行(服务端主动发起)
service main2terminal{

}